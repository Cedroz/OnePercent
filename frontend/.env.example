# Template showing which env vars this app needs. Safe to commit (no real values).
# Anyone cloning the repo copies this to .env.local and fills in their own values.
VITE_API_URL=http://127.0.0.1:8000
